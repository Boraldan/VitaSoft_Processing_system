# “естовый проект дл€ VitaSoft по управлению за€вками

## ќбща€ информаци€

ƒанный проект представл€ет собой систему управлени€ за€вками с использованием монолитной архитектуры. 
¬ системе реализованы роли пользователей: администратор, оператор и обычный пользователь. 
 аждый пользователь имеет определенные права и ограничени€, позвол€ющие им взаимодействовать с системой в зависимости от их роли.

### ќсновные функции:

- ѕользователи могут создавать, редактировать и отправл€ть за€вки.
- ќператоры могут просматривать, принимать и отклон€ть за€вки.
- јдминистраторы могут управл€ть пользовател€ми и назначать им роли.
- ¬ проекте протестированы несколько методов с использованием библиотеки Mockito.

## «апуск базы данных в Docker

ƒл€ работы проекта используетс€ база данных PostgreSQL, котора€ разворачиваетс€ с помощью Docker. Ќиже представлены шаги по запуску базы данных:

1. ”бедитесь, что у вас установлен Docker.
2. —оздайте файл `docker-compose.yml` с следующим содержимым:

```yaml
version: '3.8'

services:
  postgres:
    image: postgres:latest
    container_name: postgres_db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 123
      POSTGRES_DB: vita_db
    ports:
      - "15432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/vita_db

volumes:
  postgres_data:


